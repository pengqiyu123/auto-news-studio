# 0.2.3 更新说明

## 本次重点
- 修复微信草稿箱仅微信数据的抓取与去重问题，刷新后仅微信数量与浏览器端一致。
- 新增微信公众号发表记录检查与展示。
- 微信草稿箱改为单页切换视图，保留状态标签与本地记录对照。
- 简报左上角状态改为中文显示。
- 外部 AI 驱动说明补充到 `AGENT.md`，便于 Codex / Claude Code 直接读取真实接口。

## 兼容性
- 版本号已更新为 `0.2.3`。
- GitHub Releases 需要同步发布 `v0.2.3`，否则旧版本不会自动识别到更新。

## 备注
- 传统模式仍然保持三档自动化。
- 外部 AI 驱动仍然复用同一份 `data/state.json`。
