# Auto News Studio 调度状态显示重构计划（四态表达版）

## Summary

本轮基于已确认的启动方式和展示线框，重构首页主控区的调度状态表达。目标不是修改调度引擎，而是让小白用户也能一眼看懂：

- 当前系统是**没开**
- 还是**在等开始**
- 还是**正在跑**
- 还是**已经跑完、正在等下一轮**

固定前提：

- 当前启动方式共有 4 个：
  - `立即一次`
  - `定时一次`
  - `立即循环`
  - `定时循环`
- 这 4 个启动方式不需要 4 套完全独立 UI
- 固定收口为 **4 个显示状态块**：
  1. `已停止`
  2. `等待开始`
  3. `执行中`
  4. `执行后结果`
- 其中第 4 块按任务类型再分：
  - 一次性任务 -> `本轮完成 / 本轮失败`
  - 循环任务 -> `等待下一轮`

本轮核心原则：

- **当前调度状态** 和 **最近结果** 分开表达
- **进度条只服务活跃轮次**
- **循环任务完成后必须进入等待态表达**
- **一次性任务完成后必须回到可再次启动状态**

---

## Key Changes

### 1. 固定 4 个显示块

#### A. 已停止
用于：
- `立即一次` 启动前
- `立即循环` 启动前
- 任意模式被手动停止后

显示固定为：
- 主状态：`已停止`
- 模式标签：当前计划类型
- 主按钮：`开始监测`
- 不显示进度条
- 不显示倒计时
- 如有历史结果，可显示“最近结果摘要”

#### B. 等待开始
用于：
- `定时一次` 在到点前
- `定时循环` 在首次启动前

显示固定为：
- 主状态：
  - `等待执行`（定时一次）
  - `等待启动`（定时循环）
- 显示：
  - 开始时间
  - 距离开始倒计时
- 主按钮：
  - `停止计划`
- 不显示轮次进度条
- 不显示“本轮完成”

#### C. 执行中
用于：
- 4 种启动方式在实际跑这一轮时

显示固定为：
- 主状态：`监测中`
- 显示：
  - 当前阶段
  - 百分比
  - done/total
  - ETA
  - 当前轮次开始时间
- 显示进度条
- 主按钮：`停止监测`

固定约束：
- 活跃执行态是所有模式共用的一套 UI
- 不再按模式重复造阶段展示

#### D. 执行后结果
分成两类：

##### D1. 一次性任务结果态
用于：
- `立即一次`
- `定时一次`

显示固定为：
- 主状态：
  - `本轮完成`
  - 或 `本轮失败`
- 显示：
  - 本轮耗时
  - 素材 / 新事件 / 升温事件摘要
  - 最近完成时间
- 主按钮：
  - `开始监测`
- `停止监测` 灰掉
- 不显示“下一次运行”

##### D2. 循环任务等待态
用于：
- `立即循环`
- `定时循环`（进入正常循环后）

显示固定为：
- 主状态：`等待下一轮`
- 最近结果单独显示：
  - `上一轮完成`
  - 或 `上一轮失败`
- 显示：
  - 下一次运行时间
  - 距离下一次倒计时
  - 当前频率
  - 今日完成轮次
  - 最近一轮耗时
  - 最近一轮结果摘要
- 主按钮：
  - `停止监测`
- 次按钮：
  - `立即补跑`

固定约束：
- 循环模式下“完成”不是主状态，而是最近结果
- 主状态必须是“等待下一轮”

### 2. 当前状态与最近结果分层

顶部状态区固定拆成两层：

#### 第一层：当前调度状态
固定只显示：
- `已停止`
- `等待执行`
- `等待启动`
- `监测中`
- `等待下一轮`

#### 第二层：最近结果
固定显示：
- `上一轮完成`
- `上一轮失败`
- `尚未执行`

固定约束：
- 不允许把“本轮完成”同时当主状态又当最近结果
- 循环任务允许出现：
  - 主状态：`等待下一轮`
  - 最近结果：`上一轮完成`

### 3. 进度条重构规则

#### 活跃轮次中
保留现有进度条系统，但只在执行中显示：
- 阶段标签
- 百分比
- done/total
- ETA
- 进度条

#### 非活跃状态
固定规则：

- `已停止`：不显示进度条
- `等待开始`：不显示进度条
- `一次性任务完成后`：不显示进度条
- `循环任务等待下一轮`：不显示进度条

固定约束：
- 不允许“等待下一轮”时继续显示 `100%`
- 不允许“等待下一轮”时仍显示上一阶段名称，如 `判断热度与预警`
- `_completion_hold_until` 只允许短暂支撑“本轮刚完成”的视觉过渡，过渡后必须让 UI 切换到目标态

### 4. 按 `launch_mode` 固定按钮语义

#### `once_now`
- 启动前：`开始监测`
- 运行中：`停止监测`
- 完成后：恢复 `开始监测`

#### `once_at`
- 等待时：`停止计划`
- 运行中：`停止监测`
- 完成后：恢复 `开始监测`

#### `interval_now`
- 启动前：`开始监测`
- 运行中：`停止监测`
- 每轮完成后：保持 `停止监测` + `立即补跑`

#### `interval_at`
- 等待首次启动时：`停止计划`
- 进入循环后：`停止监测`
- 每轮完成后：保持 `停止监测` + `立即补跑`

固定约束：
- `开始监测` 在循环模式里表达的是“开启值守”
- `立即补跑` 是循环模式的附属动作，不替代开始/停止

### 5. 当前实现需要补的前端派生逻辑

基于现有字段补充以下派生层：

- `current_runtime_state`
  - `stopped | waiting_start | running | waiting_next | one_shot_done | one_shot_failed`

- `is_one_shot_mode`
  - `launch_mode in {"once_now", "once_at"}`

- `is_loop_mode`
  - `launch_mode in {"interval_now", "interval_at"}`

- `has_started_loop`
  - 用于区分 `定时循环` 是“等待首次启动”还是“已进入循环等待下一轮”

- `last_run_result_label`
  - `上一轮完成 / 上一轮失败 / 尚未执行`

- `next_run_countdown_label`
  - 基于 `next_collect_at` 实时计算

- `schedule_descriptor`
  - 例如：
    - `立即一次`
    - `今晚 21:30 执行一次`
    - `每 15 分钟`
    - `今晚 21:30 开始，每 15 分钟`

- `show_progress_meter`
  - 只在真正活跃执行时为 `true`

### 6. 对现有组件的明确改造点

#### `runtimeIntent.ts`
固定调整：
- `deriveRuntimeDisplayStatus()` 不再直接把所有 completed 统一当主状态返回
- 新增更细的派生函数，优先产出 4 个显示块语义
- `getRuntimeProgressMeta()` 在等待态和完成态下隐藏进度条
- 区分：
  - `等待开始`
  - `等待下一轮`

#### `IntelOverviewPage.tsx`
固定调整：
- 顶部状态条拆成：
  - 当前状态
  - 最近结果
- `nextRunLabel` 改成结构化等待信息区，而不是单一一句话
- 顶部按钮按 `launch_mode + 当前显示块` 切换
- 进度条区域只在执行中展示
- 循环模式完成后展示等待卡片，不展示 100% 完成条残留

#### 现有 dashboard/runtime 数据
本轮优先复用现有字段：
- `launch_mode`
- `running`
- `control_state`
- `next_collect_at`
- `completed_cycles_today`
- `failed_cycles_today`
- `last_cycle_duration_seconds`
- `last_cycle_summary`
- `run_status`
- `last_run_outcome`

默认不新增后端接口，除非实现时发现前端无法稳定区分“等待开始”和“等待下一轮”。

---

## Public Interfaces / Types

本轮默认不改 API 结构，优先在前端派生。

若实现时确实无法稳定区分状态，允许最小新增以下后端字段之一，但只作为兜底：
- `runtime_phase_kind`
  - `stopped | waiting_start | running | waiting_next | completed_once`
- 或 `is_loop_waiting`
- 或 `has_armed_start`

默认选择：**先不新增接口字段**。

---

## Test Plan

### 1. 立即一次
- 启动前显示 `已停止`
- 点击开始后进入执行中
- 完成后主状态显示 `本轮完成` 或 `本轮失败`
- `开始监测` 恢复可点
- 不显示下一次运行和倒计时

### 2. 定时一次
- 到点前显示 `等待执行`
- 显示开始时间和距离开始
- 到点后进入执行中
- 完成后回到一次性结果态
- 不进入 `等待下一轮`

### 3. 立即循环
- 启动前显示 `已停止`
- 启动后第一轮进入执行中
- 一轮完成后切换为 `等待下一轮`
- 显示：
  - 下一次运行
  - 倒计时
  - 频率
  - 今日完成轮次
- 按钮保留 `停止监测` 与 `立即补跑`

### 4. 定时循环
- 首次到点前显示 `等待启动`
- 到点后进入执行中
- 一轮完成后进入 `等待下一轮`
- 不再显示“等待启动”

### 5. 进度条
- 执行中正常显示进度条
- 完成短暂 hold 后，若是循环模式，切换为等待卡片
- 不允许等待态还显示 `100%`
- 不允许等待态还显示上一执行阶段名称

### 6. 最近结果
- 循环模式下可同时看到：
  - 当前状态：`等待下一轮`
  - 最近结果：`上一轮完成`
- 失败时同理：
  - 当前状态：`等待下一轮`
  - 最近结果：`上一轮失败`

---

## Assumptions

- 当前用户已确认：`立即循环` 与 `立即一次` 是不同心智，完成后的表现必须不同
- `开始监测` 在循环模式里等价于“开启持续监测”
- `立即补跑` 只作为循环模式中的附属动作
- 本轮优先修正首页状态表达，不调整调度核心执行逻辑
- `_completion_hold_until` 可以保留，但只作为短暂过渡，不得主导最终等待态显示
