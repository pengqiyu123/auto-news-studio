# Auto News Studio 稿件工作台清理与历史视图计划

## Summary

本轮不新增新的顶层导航，继续保持稿件层只有 `选题池 / 稿件` 两栏；要补的是 `稿件` 页内部的工作台能力，让它既能清理无用稿件，也能看清哪些已经进入微信草稿箱、哪些只是处理中、哪些失败了。

基于当前代码现状，固定决策如下：

- 新增 **稿件删除** 能力
- 不新增独立的“历史记录”侧边导航
- 在 `稿件` 页内部新增 **二级视图栏**
- 复用现有 `pipeline_stage`，**不新增 draft history 表**
- 删除首版采用 **仅删除本地记录** 语义，不尝试删除微信端草稿
- `published` 记录首版默认 **不提供删除按钮**，保留为已发布留痕

---

## Key Changes

### 1. 稿件页改成“工作台 + 历史视图”心智，但仍在同一页内完成

`稿件` 页顶部新增二级视图栏，固定为：

- `全部`
- `进行中`
- `草稿箱记录`
- `已发布`
- `失败`

固定口径：

- `进行中` = `drafted | preview_ready | approved`
- `草稿箱记录` = `draft_synced`
- `已发布` = `published`
- `失败` = `failed`，以及存在 `last_error` 且未发布的稿件优先归到失败视图
- `全部` 保留现有全量浏览能力

固定约束：

- 不新增新的顶层 sidebar 导航
- 不新增新的持久化历史表
- 仍然用现有 `DraftItem.pipeline_stage` + `last_error` 做前端派生
- 现有阶段下拉可以收口为这个二级视图栏，避免“阶段”和“历史”两套筛选打架

### 2. 新增稿件删除，但删除的是“本地稿件记录”，不是微信远端草稿

后端新增删除能力：

- `DELETE /api/admin/drafts/{draft_id}`

固定删除语义：

- 从本地 `drafts` 列表移除该稿件
- 同步刷新相关事件/候选的 `draft_exists`
- 不删除 `publish_tasks / logs / jobs`，这些继续作为运行记录保留
- 不调用微信远端删除；若已存在 `wechat_draft_id`，只删除本地记录

固定前端提示：

- 普通未同步稿件：`确认删除这篇稿件？`
- 已进微信草稿箱稿件：`仅删除本地记录，不会删除微信草稿箱中的稿件，是否继续？`
- `published` 首版不显示删除按钮，避免把发布留痕误删掉

固定交互：

- 删除按钮放在现有动作区，与 `编辑 / 重生成 / 草稿箱 / 预览` 同层
- 采用现有项目一致的轻确认方式，优先复用类似来源删除的二次确认模式
- 正在执行动作的稿件不可删除
- 删除成功后立即从当前列表消失，并给出 toast

### 3. 解决“跳到稿件页却看不到稿件”的工作台一致性问题

当从热点簇、预警页、选题池生成稿件并自动跳转到 `稿件` 页时，固定行为为：

- 自动切换到 `稿件`
- 自动切到 `进行中` 或 `全部` 视图
- 清空搜索词与其他会把新稿件过滤掉的筛选
- 高亮新稿件行
- 若稿件仍在生成中，顶部继续显示“AI 正在生成稿件”

这样可以避免“跳转成功了，但因为残留筛选条件导致用户以为没生成”。

### 4. 稿件历史的价值收口为“页内可回看”，不是“单独系统”

固定产品判断：

- `草稿箱记录` 就是首版最有用的“历史层”
- `已发布` 是更终态的运营留痕
- 不再单独加一个新的“历史记录”主导航
- 首版不新增归档、回收站、远端草稿管理

也就是说，用户在 `稿件` 页里就能完成三件事：

- 处理正在写的稿
- 回看已进草稿箱的稿
- 排查失败稿和历史发布稿

---

## Public Interfaces / Types

### 后端接口
新增：

- `DELETE /api/admin/drafts/{draft_id}`

返回建议保持轻量一致：

- `{ "ok": true }`
- 或返回删除后的最新 `DraftsResponse`，二选一；推荐 `{ "ok": true }`，前端自行刷新现有列表即可

### 前端派生类型
前端新增本地视图枚举即可，不要求后端持久化：

- `DraftWorkbenchView = "all" | "active" | "synced" | "published" | "failed"`

### 现有类型
- `DraftItem` 不要求新增字段
- 继续复用：
  - `pipeline_stage`
  - `last_error`
  - `wechat_draft_id`

---

## Test Plan

### 1. 删除能力
- 删除 `drafted` 稿件成功，本地列表立即移除
- 删除 `draft_synced` 稿件时显示“仅删除本地记录”提示
- 删除后刷新热点/候选，相关 `draft_exists` 正确回退
- `published` 稿件不出现删除按钮
- 正在 busy 的稿件不能触发删除

### 2. 稿件页历史视图
- `进行中` 只显示 `drafted / preview_ready / approved`
- `草稿箱记录` 只显示 `draft_synced`
- `已发布` 只显示 `published`
- `失败` 能看到 `failed`，以及有 `last_error` 的异常稿件
- `全部` 仍可查看所有稿件

### 3. 跳转可见性
- 从热点簇点击 `生成稿件` 后，自动进入 `稿件` 页且能看到新稿件
- 旧搜索词、旧筛选不会把刚生成的新稿件隐藏掉
- 高亮定位仍然有效
- 生成中的顶部提示保留，生成完成后消失

### 4. 兼容与回归
- 不影响现有 `编辑 / 重生成 / 草稿箱 / 预览 / 推进发布`
- 不影响微信同步链路
- 不影响 `选题池` 和热点/预警入口
- `publish_tasks / logs / jobs` 仍可保留历史记录，不因删除稿件报错

---

## Assumptions

- 本轮“历史记录”定义为稿件页内部视图，不是新的顶层导航
- 删除首版只删本地，不做微信远端草稿删除
- `published` 记录首版保留，不提供删除
- 失败视图优先服务排障，因此允许把“有错误但阶段未落到 failed”的稿件归入失败视图
- 当前项目已有的阶段模型已经够用，本轮不新增 draft history 持久层
