# Auto News Studio 监测意图层重构实施计划（最终定稿）

## Summary

本次只重构**用户控制层与结果表达层**，不调整采集、聚类、预警主流水线。

目标固定为：

- 首页只表达“开始监测”，不再让用户选择内部流水线切面
- `collect_only / collect_events / collect_events_alerts` 保留为内部能力
- 高级维护能力继续可用，但降级到折叠区
- 空状态、状态文案、按钮语义统一围绕“监测意图”表达

范围控制：

- **前端主改**
- **后端小补**
- **不动 P0/P1/P2 核心稳定性逻辑**

---

## Key Changes

### 1. 首页主控制改为意图驱动

首页主操作固定为：

- `开始监测`
- `停止监测`
- `立即补跑`
- `计划设置`
- `高级操作`

行为固定：

- `开始监测` -> 默认执行完整链路
- `立即补跑` -> 默认执行一次完整链路补跑
- 首页不再直接暴露：
  - `只采集`
  - `采集 + 事件`
  - `采集 + 事件 + 预警`

`高级操作` 折叠区固定保留：

- `仅采集素材`
- `重建事件`
- `重算预警`

内部映射固定：

- `仅采集素材` -> `collect_only`
- `重建事件` -> `collect_events`
- `重算预警` -> `collect_events_alerts`
- `开始监测 / 立即补跑` -> `collect_events_alerts`

### 2. 监测意图挂到现有 `automation_run`

不新增新的顶层 runtime 容器，直接在现有 `automation_run` 上补字段：

- `intent`
  - `normal_monitoring`
  - `collect_validation`
  - `event_rebuild`
  - `alert_rebuild`
- `last_run_outcome`

语义固定：

- `normal_monitoring`
  - 正常产品路径
- `collect_validation`
  - 素材校验
- `event_rebuild`
  - 事件重建
- `alert_rebuild`
  - 预警重算

说明：

- `normal_monitoring` 与 `alert_rebuild` 都会落到 `collect_events_alerts`
- 区别只在**产品语义、状态表达、空状态说明**
- `last_run_outcome` 记录上一轮实际完成情况

### 3. `display_status` 由前端推导

不新增后端 `display_status` 字段。

前端根据以下输入推导展示状态：

- `run_status`
- `work_scope`
- `automation_run.intent`
- `automation_run.last_run_outcome`
- 是否存在下一轮计划
- 当前是否正在执行高级操作

前端统一输出 6 种用户状态：

- `监测中`
- `等待下一轮`
- `已停止`
- `本轮完成`
- `本轮失败`
- `维护任务执行中`

推导原则固定：

- 正常链路运行中 -> `监测中`
- 高级操作运行中 -> `维护任务执行中`
- 有计划但当前 idle -> `等待下一轮`
- 手动停止 -> `已停止`
- 上一轮成功结束 -> `本轮完成`
- 上一轮异常结束 -> `本轮失败`

### 4. 空状态按“运行意图 + 最近结果”解释

空状态区分三类原因：

- **当前模式不生成**
- **本轮暂无结果**
- **趋势数据尚不足**

固定规则：

#### 实时流
- 无素材：`本轮还没有抓到新的素材。`

#### 热点簇
- 当前或上轮为 `collect_validation`：
  - `当前运行模式仅采集素材，不生成热点事件。`
- 完整监测或事件重建但为空：
  - `本轮还没有形成可聚合的热点事件。`

#### 预警台
- 当前或上轮为 `collect_validation / event_rebuild`：
  - `当前运行模式不生成预警。`
- 完整监测但快照不足：
  - `预警需要连续轮次积累趋势数据。`
- 完整监测且无触发：
  - `本轮暂无上升或爆发事件。`

约束固定：

- 首页、热点簇页、预警台页的解释必须一致
- 不允许出现“执行成功”但用户视觉理解为“系统坏了”

### 5. 后端仅补最小上下文字段

后端只做最小改动，不动核心逻辑：

- 在 `automation_run` 中写入 `intent`
- 在轮次结束时写入 `last_run_outcome`
- 保持现有：
  - `work_scope`
  - `run_status`
  - `progress`
  - `heartbeat`
  - `runtime_plan`

关键约束：

- `last_run_outcome` 必须在 `_finish_runtime_run` 统一落盘
- 必须覆盖：
  - 成功结束
  - 失败结束
  - 被新轮次接管 / abandoned
- 不要把 outcome 写散在各个分支里，避免遗漏退出路径

不改动：

- 采集逻辑
- 事件聚类逻辑
- 预警生成逻辑
- P0/P1/P2 的运行保护、心跳、僵尸回收语义

### 6. 保留内部三段式，但降级为维护能力

以下能力全部保留，不删除：

- `collect_only`
- `collect_events`
- `collect_events_alerts`

仅改变暴露方式：

- 不再作为首页主决策项
- 改为“高级操作”中的维护能力
- 首页默认永远强调完整监测

---

## Implementation Order

1. **后端**
   - 在 `automation_run` 增加 `intent`
   - 在 `_finish_runtime_run` 统一写入 `last_run_outcome`

2. **前端首页**
   - 重构 `IntelOverviewPage` 主控制区为意图驱动按钮
   - 将三段式工作内容下沉到 `高级操作`

3. **前端状态层**
   - 新增 `display_status` 推导函数
   - 用 `run_status + intent + outcome + plan` 统一生成展示状态

4. **前端结果页**
   - 热点簇、预警台、首页空状态按运行意图解释
   - 保证跨页面文案一致

5. **验证**
   - `build`
   - 手动走一遍：
     - 开始监测
     - 仅采集素材
     - 重建事件
     - 重算预警
     - 失败路径 / 停止路径 / 接管路径

---

## Test Plan

### 1. 主流程理解
- 新用户首次进入，只看到“开始监测”也能理解如何使用
- 不需要理解采集、事件、预警的内部层级

### 2. 意图映射正确
- `开始监测` 走完整链路
- `仅采集素材` 只更新实时流
- `重建事件` 生成热点簇但不生成预警
- `重算预警` 使用完整链路，但前端语义显示为维护行为

### 3. 状态显示正确
- 正常运行中显示 `监测中`
- 高级操作运行中显示 `维护任务执行中`
- 有计划但空闲时显示 `等待下一轮`
- 成功结束显示 `本轮完成`
- 异常结束显示 `本轮失败`

### 4. 空状态一致
- 素材模式下，热点簇和预警台都明确说明“当前模式不生成”
- 完整监测模式下，无结果时显示“暂无结果”
- 趋势样本不足时，预警台明确说明需要连续轮次

### 5. 兼容性
- 不影响现有实时流、热点簇、预警台的数据接口
- 不影响重点观察、稿件、来源健康页面
- 不影响 runtime plan 的保存与执行

---

## Assumptions

- `intent` 与 `last_run_outcome` 直接挂在现有 `automation_run` 下
- `display_status` 由前端推导，不新增后端字段
- 默认推荐路径固定为完整监测
- 高级操作保留，但退出首页主心智
- 本次不进入采集、聚类、预警算法层修改
